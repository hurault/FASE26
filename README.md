## This is the GitHub repository for the paper “Formally Correct Search for Interpretable DNFs”, accepted at FASE 2026.

The `Experiments` directory contains the source code and datasets, while the `Why3` directory contains the files that make it possible to replay the proofs of correctness and completeness of the SAT encoding.
For more details, please refer to the `README` files present in each directory.
